---
- hosts: all
  connection: local

  tasks:
    - name: Create dev directory
      file:
        path: "{{ home_dir }}/dev/"
        state: directory

    - import_tasks: roles/workstations/tasks/repos.yml
    - import_tasks: roles/development_workstations/tasks/repos.yml
